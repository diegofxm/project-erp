# Plan de Acción — orden de ejecución recomendado

Este plan reordena los hallazgos de los 9 informes (no por severidad aislada, sino por **dependencia real entre componentes**), para que cada fase se construya sobre una base ya corregida en vez de tener que rehacerse después.

## Lógica de secuenciación

La relación de dependencia del sistema es `frontend → erp → cofacture`: el frontend consume la API de `erp`, y `erp` consume `cofacture` como motor de facturación. Eso significa que **`cofacture` es la base de todo lo financiero/legal** — si tiene un bug de reconciliación o un dato sin cifrar, cualquier trabajo que se haga en `erp` (auditoría, RBAC, reportes) hereda ese riesgo sin saberlo, y si `cofacture` cambia de comportamiento más adelante para corregirlo, ese trabajo en capas superiores puede tener que revisarse. Por eso el orden general es **de abajo hacia arriba: Cofacture → erp → frontend → expansión de producto**, con una única excepción:

- **La fuga de secretos en git (Fase 0) no respeta esta jerarquía** — es un problema de higiene de repositorio, no de arquitectura de capas, y una credencial de base de datos real expuesta hoy es explotable independientemente de en qué capa se esté trabajando. Se resuelve primero y en paralelo a todo lo demás, no como parte de la secuencia de capas.

Dentro de cada fase, el orden interno también importa: por ejemplo, en la Fase 2 la autorización (RBAC) va antes que la auditoría porque ambas tocan los mismos handlers — conviene modificarlos una sola vez — y el CI de tests va después de escribir los tests de `electronic` (Fase 1), porque un CI que no corre nada de valor todavía no aporta.

Cada ítem indica: archivos/módulo, de qué otro ítem depende (si depende de alguno), esfuerzo cualitativo, e informe de origen.

---

## Fase 0 — Emergencia inmediata (sin dependencias, hoy mismo)

| # | Tarea | Estado | Depende de | Esfuerzo | Informe |
|---|---|---|---|---|---|
| **01** | Rotar la contraseña de Postgres en alwaysdata.net, las credenciales del proyecto Neon, `AUTH_JWT_SECRET`, `ISSUER_SECRETS_KEY` y las credenciales SMTP de Mailtrap expuestas en `_legacy/apidian/.env`/`.env.old`. Borrar ambos archivos del working tree, purgar el historial completo de git con `git filter-repo`/BFG (un `git rm` normal no basta), y corregir `.gitignore` para que `_legacy/apidian/.env*` quede cubierto. | 🟡 **Parcial** (ver nota — historial purgado; rotación de credenciales sigue pendiente, la gestiona el usuario aparte) | — | Bajo | 06 |

**Nota sobre el estado del punto 01 (2026-08-09):** `_legacy/` se eliminó del árbol de trabajo (commit `b29cf77`) pero, por decisión explícita del usuario, **sin purgar el historial de git** — los archivos `.env`/`.env.old` con las credenciales reales seguían recuperables desde commits anteriores (`git show e646a51:_legacy/apidian/.env`). Se confirmó además, preguntando directamente, que **esas credenciales (Postgres alwaysdata.net, Postgres Neon, `AUTH_JWT_SECRET`, `ISSUER_SECRETS_KEY`, SMTP Mailtrap) siguen en uso en producción hoy**. En ese momento el usuario decidió aceptar el riesgo residual sin rotarlas.

**Actualización (2026-08-10) — historial purgado.** `git filter-branch` (no había `git-filter-repo`/BFG instalados en este entorno — sin Python ni Java disponibles — así que se usó la herramienta equivalente ya incluida en git; ~330 commits, ~4 minutos) reescribió las 328 revisiones de la rama, eliminando `_legacy/apidian/.env` y `_legacy/apidian/.env.old` de **todos** los commits donde aparecían (`e646a51`, `b29cf77`). Verificado antes de publicar: `git log -- _legacy/apidian/.env` y `git log -S"[fragmento-redactado]"`/`-S"[fragmento-redactado]"` (fragmentos únicos de la contraseña de Postgres y de `AUTH_JWT_SECRET`) ya no devuelven ningún commit en la rama reescrita. Se limpiaron las referencias de respaldo que deja `filter-branch` (`refs/original/*`) y se corrió `git gc --prune=now --aggressive` para que los blobs viejos dejaran de ser alcanzables localmente, no solo invisibles en el log.

De paso, la rama se renombró de `v2/db-architecture` a `main` (decisión del usuario, GitHub pasa a ser el remoto único de trabajo) y se forzó el push del historial purgado a GitHub (`github.com/diegofxm/project-erp`). El remoto Gitea local (`origin`) se eliminó de la configuración de git a pedido del usuario, que lo va a dar de baja por su cuenta. Pendiente administrativo (no técnico): GitHub sigue marcando `v2/db-architecture` como rama por defecto del repo, así que rechaza borrarla (`refusing to delete the current branch`) — hay que cambiar la rama por defecto a `main` desde Settings → Branches en la web de GitHub (no hay `gh` CLI instalado en este entorno para hacerlo por API) antes de poder eliminar la rama vieja.

**Lo que sigue sin rotar, y por qué:** el usuario confirmó explícitamente que la rotación de las credenciales activas (contraseña de Postgres en alwaysdata.net, `AUTH_JWT_SECRET`, `ISSUER_SECRETS_KEY`, SMTP Mailtrap) **ya la está gestionando por su cuenta, fuera de esta sesión** — no se generaron valores nuevos aquí a propósito, para no crear una segunda fuente de verdad desincronizada con lo que el usuario vaya a aplicar en los paneles de alwaysdata.net/Neon/Mailtrap y en el VPS. El proyecto Neon (evaluado pero nunca adoptado oficialmente, ver punto 18) tampoco se tocó. Este punto queda parcial, no resuelto: el vector de "credencial recuperable desde el historial de git público" ya se cerró, pero las credenciales en sí mismas siguen siendo las mismas hasta que el usuario complete la rotación por su lado.

---

## Fase 1 — Cofacture: la base de la que depende todo lo financiero/legal

| # | Tarea | Estado | Depende de | Esfuerzo | Informe |
|---|---|---|---|---|---|
| **02** | Reconciliar con `GetStatus`/`GetStatusZip` antes de liberar el consecutivo cuando `sendSync` falla por timeout/error de transporte (`confirm.go`, función `markError`/`finish`) — hoy se libera el número sin confirmar si la DIAN sí procesó el documento. | ✅ **Resuelto** (ver nota) | — | Medio | 04 |
| **03** | Mecanismo de reintento/contingencia real para el envío síncrono: mover a la ruta asíncrona (`SendBillAsync` + `GetStatusZip`, que ya tiene un identificador para consultar después) o agregar un job de reintento con backoff. | ✅ **Resuelto** (ver nota) | 02 (comparten la lógica de reconciliación) | Medio-Alto | 04 |
| **04** | Cifrar el blob del certificado `.p12` en BD con la misma capa AES-256-GCM (`cryptutil`) ya usada para `certificate_password`/PINes. | ✅ **Resuelto** (ver nota) | — | Bajo | 04, 06 |
| **05** | Registrar un estado dedicado (`StatusEnvironmentMismatch` o similar) cuando el ambiente del rango de numeración no coincide con el de la empresa, en vez de dejar el documento atascado en `built` sin ningún error visible. | ✅ **Resuelto** (ver nota) | — | Bajo | 04 |
| **06** | Reemplazar el `PostalZone` fijo (`"000000"`) por captura/validación del dato postal real en `thirdparty`. | ✅ **Resuelto** (ver nota — alcance distinto al previsto) | — | Bajo (terminó siendo más chico de lo estimado) | 04 |
| **07** | Tests de aplicación para `electronic/application/confirm.go`: decisión síncrona/asíncrona, manejo de `TestSetID`, y el nuevo comportamiento de reconciliación de 02/03/05. | ✅ **Resuelto** (ver nota) | 02, 03, 05 (para testear el comportamiento correcto, no el que se está reemplazando) | Medio | 07 |

**Nota sobre el punto 02 (2026-08-09):** el título original decía "reconciliar con `GetStatus`/`GetStatusZip`", pero al implementarlo se confirmó que eso **no es mecánicamente posible para `SendBillSync`**: el endpoint síncrono de la DIAN no devuelve ningún identificador (trackID/ZipKey) hasta que responde completo, así que si el `http.Client` agota el timeout esperando esa respuesta, no queda nada que consultar después — `GetStatus`/`GetStatusZip` solo sirven para las rutas asíncronas (`SendBillAsync`/`SendTestSetAsync`), que sí entregan un ZipKey de entrada. Dado eso, la corrección real implementada fue:
- El adaptador (`erp/internal/electronic/infrastructure/cofacture/adapter.go`, funciones `SendBillSync`/`SendTestSetAsync`) ahora distingue si el error es un `soap.Fault` explícito (la DIAN sí respondió y rechazó a nivel de protocolo — sin ambigüedad) usando `errors.As`, y en ese caso lo envuelve con el nuevo sentinel `domain.ErrDianRejectedSync`.
- `ConfirmUseCase.markError` (`erp/internal/electronic/application/confirm.go`) usa `errors.Is(sendErr, domain.ErrDianRejectedSync)`: si es un fault explícito → `StatusSendError` (comportamiento de siempre, libera el consecutivo). Si es cualquier otro error (timeout, conexión, respuesta ilegible) → nuevo estado `domain.StatusSendUnknown`, que **no libera el consecutivo** en `finish()`.
- El error de empaquetado ZIP (`uc.zipper.Zip(...)`, que ocurre *antes* de cualquier llamada a la DIAN) se separó para ir directo a `StatusSendError` sin pasar por esta ambigüedad — ahí sí hay certeza absoluta de que nunca se envió nada.
- Se agregó el estado nuevo en el frontend (`types.ts`, `StatusBadge.tsx` con tono *warning*, filtros de las 5 páginas de documentos) y en las métricas de `stats` (se cuenta junto a `rejected`/`send_error` para no desaparecer del dashboard).
- Verificado con `go build ./... && go vet ./... && go test ./...` (limpio, sin `FAIL`) y `npm run build` en frontend (sin errores de tipos).
- **Limitación conocida, que queda para el punto 03**: un documento en `StatusSendUnknown` no tiene hoy ningún camino automático de resolución — el consecutivo queda bloqueado indefinidamente hasta una revisión manual (verificar en el portal de la DIAN) o una intervención directa en BD. No se construyó un endpoint de resolución manual en este punto para no exceder el alcance — es candidato natural para cuando se aborde el punto 03 (mecanismo de reintento/contingencia real).

**Nota sobre el punto 03 (2026-08-09):** se eligió "agregar un reintento" en vez de "migrar producción entera a la ruta asíncrona", precisamente para no cambiar la latencia del caso feliz (una confirmación exitosa por `SendBillSync` sigue respondiendo igual de rápido que siempre). El reintento solo se activa en el caso de error:
- `SenderPort` ganó `SendBillAsync` (puerto + adaptador, mismo patrón que `SendTestSetAsync`, con la misma distinción `soap.Fault` vs. error de transporte del punto 02).
- `ConfirmUseCase.sendSync`: si `SendBillSync` falla con un error ambiguo (no `ErrDianRejectedSync`), ya no se rinde — reintenta reenviando el **mismo ZIP ya firmado** (mismo CUFE, no un documento nuevo) por `SendBillAsync`, y si eso da un `zipKey`, hace el mismo sondeo acotado (6×5s) que ya usaba la ruta de habilitación. Si el reintento también resulta ambiguo, el documento queda en `StatusSendUnknown` (no libera el número) con ambos mensajes de error conservados. Si el sondeo se agota sin respuesta, queda en `StatusSent` con el `zipKey` guardado — recuperable, a diferencia de `StatusSendUnknown`.
- Se refactorizó el sondeo (`pollZipKey`/`finishFromPoll`) para compartirlo entre `sendTestSet` y el nuevo reintento, sin duplicar la lógica.
- **Se cerró el dead-end que ya existía en `StatusSent`** (afectaba también a la ruta de habilitación, no solo a la nueva): antes, si el sondeo se agotaba, el documento quedaba ahí para siempre sin ningún código que lo volviera a consultar. Nuevo caso de uso `ConfirmUseCase.CheckPendingStatus(companyID, id)` + endpoint `POST /api/v1/electronic/documents/{id}/check-status` + botón "Consultar estado" en las 5 páginas de edición de documentos (visible solo cuando `status == "sent"`) — permite volver a sondear con el mismo `zipKey` sin generar ningún envío nuevo.
- Verificado con `go build/vet/test` (limpio) y `npm run build` (sin errores de tipos).
- **Limitación que queda abierta**: todo lo anterior es a demanda (el reintento automático ocurre en el momento del fallo; `CheckPendingStatus` requiere que un usuario haga clic). No hay ningún job en segundo plano que barra periódicamente los documentos en `StatusSent` sin intervención humana — sería la evolución natural si se quiere contingencia 100% automática, pero se dejó fuera de este punto para no sumar una pieza de infraestructura nueva (scheduler cross-tenant) al mismo cambio. (Nota: no quedó como ítem numerado del plan, ver conversación del 2026-08-09 — no se le asignó fecha de revisión, se recomendó revisarlo solo si en producción real empiezan a acumularse documentos en `StatusSent`.)

**Nota sobre el punto 04 (2026-08-09):** `erp/internal/company/infrastructure/persistence/postgres/repository.go` — `Save`, `UpdateCredentials` y `scanCompany` ahora cifran/descifran la columna `certificate` con `cryptutil.Encrypt`/`Decrypt` (misma clave `ENCRYPTION_KEY` ya usada para `certificate_password`/PINes), en vez de guardar el blob PKCS12 crudo. **Cambio no retrocompatible**: un certificado guardado bajo el esquema anterior fallaría al descifrarse (el blob no es un ciphertext AES-GCM real, la verificación de integridad de GCM lo rechaza) — se confirmó directamente con el responsable del proyecto que **hoy no hay ninguna empresa real (ni local ni en producción) con un certificado ya subido**, así que no hizo falta backfill ni migración de datos existentes. Si en el futuro se despliega esto sobre una base de datos con certificados ya cargados, hay que cifrarlos en el mismo momento del despliegue (o pedir que se vuelvan a subir) — no hacerlo así rompería la firma/envío de documentos para esas empresas. Verificado con `go build/vet/test` (limpio).

**Nota sobre el punto 05 (2026-08-09):** `erp/internal/electronic/domain/document.go` gana `StatusEnvironmentMismatch`. En `finalizeAndSend` (`confirm.go`), cuando `p.company.Environment != p.nr.Environment`, antes se retornaba el documento en `StatusBuilt` silenciosamente (sin error, sin transición futura — quedaba atascado para siempre sin que nadie se enterara salvo notando que nunca cambiaba de estado). Ahora pasa por `finish(...)` con el nuevo estado y un mensaje explícito ("el ambiente del rango de numeración (X) no coincide con el de la empresa (Y)..."), y — a diferencia de `StatusSendUnknown` — **sí libera el consecutivo**, porque aquí no hay ninguna ambigüedad: el documento nunca llegó a transmitirse a la DIAN (el error se detecta antes de empaquetar/enviar), así que no hay riesgo de doble facturación al liberar el número. Estado propagado a frontend (`StatusBadge` con tono de error — a diferencia de `send_unknown` esto sí es un fallo cierto, no ambiguo — y filtros de las 5 páginas de documentos) y a `stats`. Verificado con `go build/vet/test` y `npm run build` (ambos limpios).

**Nota sobre el punto 06 (2026-08-09) — el alcance real terminó siendo distinto (y más chico) al descrito originalmente.** El título decía "captura/validación del dato postal real en `thirdparty`", lo que hacía pensar en agregar un campo nuevo de código postal a `thirdparty.Party` (migración + formulario). Al investigar se encontró que **el código ya tiene un patrón establecido y funcionando** para este mismo problema: `erp/internal/electronic/application/from_sale.go:111` y `from_purchase.go:119` (los flujos de "generar factura desde una venta"/"generar documento soporte desde una compra") ya usan `MunicipalityCode` (código DANE de municipio, un dato real que toda empresa/tercero ya tiene) como valor de `PostalZone` — no es una convención inventada para esta sesión, ya está en producción para esos dos flujos. El problema real era que las dos funciones de `confirm.go` (`partyFromCompanyAsNIT` para la empresa, `supplierAsNIT` para el tercero en documento soporte) **no seguían ese mismo patrón** y usaban `"000000"` fijo en su lugar — una inconsistencia interna, no la ausencia total de un dato real. La corrección fue alinear ambas funciones al patrón ya existente (`p.Address.PostalZone = firstNonEmpty(c.MunicipalityCode, "000000")` / con `p.Address.CityCode` para el tercero), dejando `"000000"` solo como último recurso si ni el municipio está registrado. No hizo falta ninguna migración de BD ni cambio de formulario — el dato ya se estaba capturando, solo no se estaba usando donde hacía falta. Verificado con `go build/vet/test`.

**Nota sobre el punto 07 (2026-08-10).** 19 tests nuevos en `erp/internal/electronic/application/confirm_test.go`, con fakes para los 5 puertos que usa `ConfirmUseCase` (`DocumentRepository`, `NumberingRepository`, `CompanyPort`, `BuilderSignerPort`, `ZipperPort`, `SenderPort`) — mismo patrón ya usado en `sales/application/confirm_test.go` (paquete `application_test`, caja negra, sin mocks generados). Cobertura:

- **Validación de entrada**: empresa incorrecta (`ErrDocumentNotFound`), documento que ya no está en borrador (`ErrDocumentNotDraft`), NC sin `BillingReference`, DS sin `Supplier`.
- **Decisión síncrona/asíncrona**: producción siempre usa `SendBillSync` aunque el rango tenga `TestSetID` (la condición real es `Environment == Produccion || TestSetID == ""`, no solo la ausencia de `TestSetID`); habilitación sin `TestSetID` también va por síncrono; habilitación con `TestSetID` usa `SendTestSetAsync` + sondeo.
- **Manejo de `TestSetID`**: si el sondeo devuelve `IsTestSetClosed`, se limpia el `TestSetID` del rango (`ClearTestSetID`) y se reintenta por la vía síncrona en la misma llamada — se verifica que ambas rutas del sender se invocan en el orden correcto.
- **Reconciliación (puntos 02/03/05)**: rechazo explícito de la DIAN (`ErrDianRejectedSync`) → `StatusSendError` + libera el consecutivo, sin reintento asíncrono (no hay ambigüedad que resolver); error de transporte ambiguo → reintento automático por `SendBillAsync`, con 3 desenlaces cubiertos por separado (reintento acepta → `StatusAccepted`, no libera; reintento también ambiguo → `StatusSendUnknown`, NO libera — el caso que motivó el punto 03; reintento rechazado explícitamente → `StatusSendError`, libera; sondeo del reintento se agota → `StatusSent` recuperable con el `zipKey` guardado); `EnvironmentMismatch` libera el consecutivo sin intentar ningún envío. `CheckPendingStatus` (resolución manual de un `StatusSent`) cubierto con su propio error de precondición y su propio caso de éxito.
- **Cambio de testabilidad, no de comportamiento**: `pollZipKey` tenía `time.Sleep(5 * time.Second)` fijo entre los 6 intentos de sondeo — se extrajo a una variable exportada `application.PollInterval` (sigue siendo 5s por defecto en producción) para que el test del caso "sondeo agotado" no tuviera que esperar ~25s reales; los tests que sí reciben respuesta en el primer intento no necesitan tocar esta variable porque `pollZipKey` nunca duerme antes de la primera llamada.

Verificado con `go build ./... && go vet ./... && go build -tags=integration ./... && go test ./...` (limpios, incluidos los 19 tests nuevos, ~3s de suite completa del paquete gracias al `PollInterval` acortado).

---

## Fase 2 — erp (backend): autorización, trazabilidad y confiabilidad de la plataforma

Una vez el motor de facturación es confiable, lo que bloquea producción es que **cualquier usuario autenticado puede saltarse permisos por API** y que **los fallos de negocio no dejan rastro** — esto es transversal a todos los módulos de `erp`, así que se resuelve antes de seguir puliendo módulos individuales.

| # | Tarea | Depende de | Esfuerzo | Informe |
|---|---|---|---|---|
| **08** | Aplicar autorización real (RBAC) de forma sistemática: definir qué acciones son "administrativas" por módulo y envolverlas con `requireManage`/`CanManage` — empezando por `sales.handleCancel`, recepción/pago en `purchase`, anulación en `electronic` (hoy solo se oculta en el frontend, el backend no valida nada en estos casos). | ✅ **Resuelto** (ver nota — barrido completo de los 7 módulos que quedaban pendientes) | — | Alto (transversal) | 09, 03 |
| **09** | Cerrar el hueco de auditoría en `electronic` (emisión/anulación DIAN, 0 de 26 handlers) y `security` (login/cambio de contraseña/invitación, 0 de 10 handlers). | ✅ **Resuelto** (ver nota — el "0 de 26"/"0 de 10" del título ya era impreciso antes de empezar) | 08 (mismos handlers, conviene tocarlos una sola vez) | Medio | 09 |
| **10** | Propagar los errores del bus de eventos (`on_sale_confirmed`, `on_purchase_received`, etc.) al caso de uso que publica en vez de solo `log.Printf`; decidir por evento si debe abortar la operación o solo advertir; registrar la falla en `audit.events`. | ✅ **Resuelto** (ver nota) | 09 (usa la infraestructura de auditoría ya reforzada) | Medio | 03 |
| **11** | Reemplazar los 14 `log.Printf` de esos mismos handlers por el logger estructurado del proyecto, y pasar el `context.Context` real de la request en vez de `context.Background()`. | ✅ **Resuelto** (ver nota) | 10 (se tocan los mismos archivos) | Bajo | 03 |
| **12** | Middleware de `recover()` centralizado con request-ID en la cadena HTTP; envolver `RunTRMDailySync` con `defer recover()` (única goroutine de larga vida sin protección hoy). | ✅ **Resuelto** (ver nota) | — | Bajo-Medio | 03 |
| **13** | Corregir el CORS "fail-open" (no activar wildcard automáticamente si falta la variable de entorno) y agregar cabeceras de seguridad HTTP básicas (`X-Content-Type-Options`, `X-Frame-Options`, CSP). | ✅ **Resuelto** (ver nota) | — | Bajo-Medio | 03, 06 |
| **14** | Rate limiting/bloqueo progresivo en login; endpoint de logout con invalidación server-side; invalidar sesiones activas al cambiar contraseña. | ✅ **Resuelto** (ver nota — decisión de arquitectura consultada antes de implementar) | — | Medio | 06 |
| **15** | Resolver las 3 violaciones de encapsulamiento de schema (`company`→`security.user_companies`, `audit`→`security.users`, `stats`→`electronic.documents`) creando puertos locales, siguiendo el mismo patrón que ya usan `sales`/`purchase`/`electronic`. | ✅ **Resuelto** (ver nota) | — | Medio | 01, 02 |
| **16** | Ajustes de base de datos: `.down.sql` faltantes (`electronic`, `hr`, `payroll`, `saas`), `UNIQUE(company_id, number)` en `sales.sales`/`purchase.orders`, índices en FKs de `electronic.documents`, `MaxConns`/`MinConns` explícitos en el pool. | ✅ **Resuelto** (ver nota) | — | Bajo-Medio | 02, 08 |
| **17** | CI mínimo: `go test ./... -tags=integration` con Postgres de servicio + `go vet` + `govulncheck` + `npm audit`, en cada push. | ✅ **Resuelto** (ver nota — sin esperar el punto 07, que sigue pendiente) | 07 (para que ya exista algo de valor que el CI corra en `electronic`) | Medio | 07, 08 |
| **18** | Healthcheck real (`pool.Ping` en `/health`); resolver la discrepancia de infraestructura documentada (Railway vs. Neon/systemd) y documentar/verificar la estrategia de backup real; versionar el script de deploy y el unit file de systemd. | 🟡 **Parcial** (ver nota — backup real queda pendiente, es decisión del usuario, no una tarea técnica que se pueda completar sin esa decisión) | 17 (mismo esfuerzo de "hacer reproducible el despliegue") | Medio-Alto | 08 |
| **19** | Flujo mínimo de Habeas Data (Ley 1581) en `thirdparty`: consentimiento, exportación de datos del titular, procedimiento de derechos ARCO. | ✅ **Resuelto** (ver nota) | — | Medio | 06 |
| **20** | Actualizar `react-router` a `>=7.18.2` (CVE alto real) y correr `npm audit fix` para `postcss`/`nanoid` en `frontend`. | ✅ **Resuelto** (ver nota) | — | Bajo | 06 |

**Nota sobre el punto 08 (2026-08-09) — resuelto parcialmente, con decisiones explícitas del responsable del proyecto.** Antes de tocar código se preguntó directamente qué rol debería poder hacer cada una de las 3 acciones nombradas en el punto, porque es una decisión de negocio (afecta el trabajo diario del equipo), no solo un fix de seguridad — hasta ahora en este proyecto "administrativo" (`requireManage`/`useCanManage`) solo se había usado para configuración de empresa, nunca para operaciones de ventas/compras. Resultado:
- **`sales.handleCancel`**: ahora requiere `requireManage` (owner/admin) — decisión explícita: cancelar una venta confirmada reversa efectos contables/inventario reales. Botón "Cancelar" en `SaleEditorPage.tsx` ahora oculto tras `useCanManage()`, consistente con el criterio "ocultar, no deshabilitar" ya usado en `issuer-settings`.
- **`purchase.handleReceive`/`handleRecordPayment`**: **sin cambios, deliberadamente** — decisión explícita: es trabajo operativo normal de compras/bodega, restringirlo a admin frenaría la operación diaria.
- **`purchase.handleCancel`**: no se preguntó explícitamente, se infirió por analogía directa con `sales.handleCancel` (mismo tipo de acción, mismo impacto) y se aplicó la misma restricción + ocultamiento del botón en `PurchaseOrderEditorPage.tsx`. Queda anotado como inferencia, no como respuesta directa — revertir si no es la intención real.
- **"anulación en `electronic`"**: se encontró que **este endpoint no existe** — no hay ningún `handleVoid`/`handleAnnul` en el módulo `electronic` hoy (NC/ND son documentos nuevos que referencian al original, no una anulación directa del documento). El punto 08 original asumía una funcionalidad que no está construida; queda como corrección de la auditoría, no como pendiente de RBAC.

**Lo que seguía pendiente y era el grueso real del punto 08**: el barrido sistemático completo — `thirdparty`, `electronic` (fuera de lo de arriba), `inventory`, `hr`, `payroll`, `product` y `catalog` seguían sin usar `CanManage` ni una vez en ningún endpoint de escritura. Cada acción nueva requeriría la misma conversación explícita que se tuvo aquí antes de decidir el rol correcto — no es algo que deba inferirse solo del código.

**Actualización (2026-08-10) — barrido completo de los 7 módulos restantes.** Inventario primero (agente de exploración, solo lectura): 16 endpoints de escritura en total repartidos en 6 módulos (`catalog` no tiene ninguno — es 100% catálogo de referencia DIAN/DANE poblado por seed, sin HTTP de escritura). De esos 16, la mayoría son operativos rutinarios (altas/ediciones de terceros, productos, movimientos de inventario) y se dejaron abiertos por el mismo criterio ya usado en `purchase.handleReceive`/`handleRecordPayment` — inferido por analogía, sin volver a preguntar, igual que se hizo con `purchase.handleCancel` en la ronda anterior:

- **`thirdparty`** (create/update/delete de customers y suppliers): sin cambios, deliberado — alta/edición/baja de un tercero es trabajo comercial normal, no una acción administrativa.
- **`inventory`** (`handleMove`, entrada/salida/transferencia/ajuste de stock): sin cambios, deliberado — mismo criterio que la recepción de compras.
- **`product`** (create/update/delete): sin cambios, deliberado — mantenimiento normal de catálogo.

Dos casos SÍ se preguntaron explícitamente antes de tocar código, porque el impacto de negocio no era inferible del código (mismo criterio metodológico de la ronda anterior):

- **`electronic` — rangos de numeración** (`handleCreateNumberingRange`/`handleDeactivateRange`/`handleActivateRange`): no se preguntó, se aplicó `requireManage` directamente — ya estaba señalado como "candidato para el barrido sistemático" desde el punto 09 original, por ser un recurso legalmente sensible (una resolución DIAN mal asignada puede invalidar documentos ya emitidos). Nuevo helper local `requireManage` en `electronic/interfaces/http/handlers.go`, mismo patrón que `sales`/`purchase`.
- **`hr` — aprobación de ausencias**: se preguntó porque hoy no existe un rol intermedio tipo "jefe de equipo" (solo owner/admin de toda la empresa), así que restringir a admin podía chocar con el flujo real de compañías más grandes. El responsable del proyecto confirmó restringir `handleApproveAbsence`/`handleRejectAbsence` a admin/owner — evita que un empleado apruebe su propia solicitud o la de un par. `handleRequestAbsence` (crear la solicitud) queda abierto.
- **`payroll`** (crear/desactivar empleados, crear/terminar contratos, generar/aprobar/pagar/anular liquidaciones): se preguntó si restringir todo el módulo o solo las acciones irreversibles/financieras. El responsable del proyecto eligió restringir **el módulo completo** — datos salariales confidenciales y compromisos legales desde la propia alta del empleado/contrato, no solo el pago. Los 8 endpoints de escritura del módulo quedaron detrás de un nuevo `requireManage` local.

**Frontend**: de los 7 módulos, solo `electronic` (rangos de numeración) tiene página propia hoy — `hr`/`payroll` todavía no tienen UI (confirmado con búsqueda exhaustiva, 0 archivos), así que no hay nada que ocultar ahí todavía; quedará cubierto cuando se construya esa UI (ver punto 26 del roadmap). `NumberingRangesPanel.tsx` ganó `useCanManage()`: los botones "Sincronizar con DIAN"/"Nuevo rango" y las 3 acciones por fila (desactivar/archivar/reactivar) ahora se ocultan para quien no sea owner/admin — mismo criterio "ocultar, no deshabilitar" ya usado en el resto del proyecto.

**Verificado en vivo, no solo compilación**: se levantó el servidor real y se generó un segundo token con el mismo `TokenService.Issue` de producción (mismo `uid`/`cid`/`token_version` reales, solo con `role="member"` en vez de `"owner"`) para probar el camino negativo sin tener que construir un usuario `member` completo. Con el token owner: `POST /electronic/numbering-ranges` → `201`, `POST /payroll/employees` → `201`, `POST /hr/absences/{id}/approve` → `404` (ID inexistente, pero no `403` — confirma que el owner sí pasa la autorización). Con el token member: los 3 mismos endpoints → `403`; de control, `POST /hr/absences` (crear, sin restringir) → `201`, confirmando que el gateo es específico y no bloqueó de más. Los registros de prueba creados durante la verificación se borraron de la base de datos local al terminar. `go build`, `go vet`, `go build -tags=integration`, `go test ./...` y `npm run build`/`npm run lint` (frontend) limpios.

Con esto el punto 08 queda completamente resuelto: los 3 ejemplos originales (ronda anterior) + los 7 módulos de esta ronda cubren el 100% de los módulos con persistencia propia. `catalog` no necesitaba nada (sin endpoints de escritura).

**Nota sobre el punto 09 (2026-08-09) — el "0 de 26"/"0 de 10" del título ya era inexacto antes de tocar nada.** Al revisar el código real de `electronic` se encontraron 6 llamadas de auditoría ya existentes (`document.confirmed`, `document.deleted`, y `logDoc` en crear/actualizar factura), y en `security` otras 4 (`auth.login`, `auth.invite_accepted`, `auth.company_selected`, `auth.user_invited`) — el hallazgo original de la auditoría de roadmap sobreestimó el hueco. El problema real era **cobertura desigual dentro de cada módulo**, no ausencia total:
- **`electronic`**: solo `handleCreateInvoiceDraft`/`handleUpdateInvoiceDraft` llamaban a `logDoc` — las mismas operaciones para Nota Crédito, Nota Débito, Documento Soporte y Nota de Ajuste no lo hacían. Se agregó `logDoc` a las 8 handlers de crear/actualizar restantes, más `handleSendDocumentEmail` (`document.email_sent`), `handleCreateInvoiceFromSale`/`handleCreateSupportDocFromPurchase` (`document.created`), y las 3 mutaciones de rangos de numeración (`numbering_range.created`/`.deactivated`/`.activated` — legalmente significativas, son la asignación real de resoluciones DIAN).
- **`security`**: faltaba `handleChangePassword` (`auth.password_changed`) y `handleUpdateProfile` (`auth.profile_updated`). `handleRegister` sigue sin loguear a propósito — `logAuth` es un no-op mientras no haya empresa activa (`audit.events` es por empresa), y un usuario recién registrado todavía no tiene una; esto ya estaba documentado como limitación conocida, no es un olvido de esta corrección.
- Verificado con `go build/vet/test` (limpio). Sin cambios de frontend — es auditoría pura, no afecta ningún flujo visible para el usuario.

**Hallazgo adicional de paso (no corregido, fuera de alcance de este punto)**: `handleCreateNumberingRange`/`handleDeactivateRange`/`handleActivateRange` usan `mustTenant` (cualquier usuario autenticado), no `requireManage` — es del mismo tipo de gap que el punto 08, sobre un recurso legalmente sensible (rangos de numeración DIAN). Queda como candidato para el barrido sistemático del punto 08, no se decidió ni se tocó aquí.

**Nota sobre el punto 10 (2026-08-09).** `erp/internal/shared/events/bus.go`: `Handler` ahora es `func(event Event) error` y `Publish` devuelve `[]error` en vez de nada — ya no descarta lo que devuelven los 7 suscriptores (`accounting`×5, `inventory`×2). Nuevo archivo `erp/internal/shared/events/audit.go` con `PublishAndAudit(ctx, bus, evento, audit, companyID, action, resourceType, resourceID)`: publica, y si algún suscriptor falla, registra la falla en `audit.events` con el mensaje combinado de todos los errores — se usa en los 4 publicadores existentes (`sales.ConfirmUseCase`, `sales.PaymentUseCase`, `purchase.ReceiveUseCase`, `purchase.PaymentUseCase`, `payroll.PayslipUseCase.Approve`).

**La decisión "¿abortar o advertir?" para los 4 eventos existentes fue la misma: advertir, no abortar.** No fue una decisión superficial — los 4 casos (confirmar venta, recibir compra, registrar pago, aprobar nómina) ya persisten el cambio principal en su propia tabla (`sales.sales`, `purchase.orders`, etc.) **antes** de publicar el evento; abortar en este punto exigiría además revertir ese cambio ya guardado (una venta que se "des-confirma" sola porque el asiento contable falló), lo cual no existe hoy como mecanismo y construirlo sería un cambio de alcance mucho mayor (compensación transaccional cross-schema) que el que pide este punto. La opción tomada dentro de lo que sí es seguro construir ahora: que la falla no se pierda en un log que nadie mira, sino que quede registrada en `audit.events` de la empresa, visible en el panel — el arreglo real del "no se rompe silenciosamente" es que ahora HAY UN RASTRO, no que la operación se cancele sola.

Cada `subscriber` (los 7 en `accounting`/`inventory`) sigue haciendo su propio `log.Printf` además de devolver el error — se dejó así a propósito para no adelantar el punto 11 (reemplazar `log.Printf` por el logger estructurado), que sigue pendiente.

Verificado con `go build`, `go vet`, `go build -tags=integration` y `go test ./...` (limpios, incluido el test existente de `sales/application/confirm_test.go` actualizado a la nueva firma).

**Nota sobre el punto 11 (2026-08-09).** Dos cambios, en los mismos 7 archivos que tocó el punto 10:
- **`context.Context` real**: `events.Handler` pasó a `func(ctx context.Context, event Event) error` y `Bus.Publish(ctx, e)` recibe y reenvía el contexto de quien publica — los 7 suscriptores ya no arman su propio `context.Background()`, usan el `ctx` real de la request HTTP que disparó la operación (a través de `Execute(ctx, ...)` → `PublishAndAudit(ctx, ...)` → `Publish(ctx, ...)` → cada `Handler`).
- **Logger estructurado**: los 14 `log.Printf` de `accounting/application/on_*.go` (×5) e `inventory/application/on_*.go` (×2) se reemplazaron por `*slog.Logger` inyectado por constructor (`logger.New("accounting")`/`logger.New("inventory")`, wireados en `main.go` antes de las 7 suscripciones).

**Hallazgo adicional descubierto al tocar estos archivos, corregido de una vez (no estaba en el alcance original del punto, pero dejarlo habría vaciado de sentido el fix del punto 10 para estos 2 casos específicos)**: en los dos suscriptores de `inventory` (`on_sale_confirmed.go`, `on_purchase_received.go`), el `log.Printf` por línea de producto vivía DENTRO del bucle `for _, line := range ev.Lines`, y `handle()` seguía devolviendo `nil` incondicionalmente al final — es decir, un fallo al mover una línea de inventario nunca llegaba a propagarse ni siquiera después del punto 10, porque `handle()` nunca lo reportaba como error de la función. Se cambió para acumular los errores por línea (`var errs []error` + `errors.Join(errs...)` al final) en vez de tragarlos silenciosamente — ahora si falla el movimiento de una línea, sí llega a `PublishAndAudit` y queda registrado en `audit.events`, igual que los demás 5 suscriptores.

Verificado con `go build`, `go vet`, `go build -tags=integration` y `go test ./...` (limpios).

**Nota sobre el punto 12 (2026-08-09).** Dos partes:
- **Middleware HTTP**: nuevo `erp/internal/shared/logger/recover.go` con `WithRequestID` (genera un ID por request si el cliente/proxy no mandó uno vía `X-Request-Id`, lo expone en `RequestID(ctx)` y en la cabecera de respuesta) y `Recover(log)` (envuelve con `recover()`, loguea `request_id`/método/path/panic/stack, responde `500` JSON en vez de dejar la conexión rota). Cadena de middleware en `main.go` quedó `recover → request-id → cors → logger → tenant → mux` (antes: `cors → logger → tenant → mux`, sin recover ni request-id). `logger.Middleware` (el que ya logueaba cada request) ahora incluye `request_id` en su línea, para poder correlacionar todas las líneas de una misma petición.
- **`RunTRMDailySync`**: se separó el cuerpo de cada intento a una función `runOnceSafely` con su propio `defer recover()`. Importante entender por qué esto es crítico y no cosmético: en Go, un panic sin recuperar en **cualquier** goroutine —no solo la que lo lanza— termina **todo el proceso**, así que un panic en la sincronización diaria de TRM (ej. nil pointer en el cliente TRM) tumbaba el servidor HTTP completo, no solo esa tarea. Envolver solo el intento (no todo el `for` de `RunTRMDailySync`) también evita que un panic aislado mate el scheduler para los días siguientes — el bucle exterior sigue esperando al próximo 1:00 a.m. con normalidad.
- **Verificado en vivo** (no solo compilación): se levantó el servidor real y se confirmó con `curl` que `/health` y un intento de login devuelven la cabecera `X-Request-Id` (valor distinto por request) y que el log del servidor incluye `request_id=...` en cada línea — sin que el comportamiento normal (200/401) se haya alterado.

**Nota sobre el punto 13 (2026-08-09).** `erp/internal/shared/cors/cors.go` se reescribió: `Middleware` ya no lee `os.Getenv` internamente ni activa wildcard si la variable está vacía — ahora recibe `allowedOrigins []string` y `devMode bool` ya resueltos. En `main.go`, `CORS_ALLOWED_ORIGINS` se resuelve con `mustEnv` (mismo patrón que `JWT_SECRET`/`ENCRYPTION_KEY` — el servidor **no arranca** si falta) salvo que se active explícitamente `CORS_DEV_MODE=true` (refleja cualquier origen, con un `log.Warn` explícito al arrancar para que quede claro en los logs que el modo permisivo está activo a propósito). Se confirmó que el `.env` local ya tenía `CORS_ALLOWED_ORIGINS` configurada, así que este cambio no rompió el entorno de desarrollo existente.

Nuevo paquete `erp/internal/shared/secheaders` con un middleware que agrega `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY` y `Content-Security-Policy: default-src 'none'; frame-ancestors 'none'` a toda respuesta — la CSP puede ser así de estricta porque este backend es una API JSON pura, no sirve HTML/JS propio (el frontend es una SPA aparte). Cadena de middleware quedó `recover → request-id → secheaders → cors → logger → tenant → mux`.

**Verificado en vivo**: con el servidor real, un origen permitido (`http://localhost:5173`) recibe `Access-Control-Allow-Origin`/`Access-Control-Allow-Credentials`; un origen no listado (`http://evil.com`) no los recibe. Las 3 cabeceras de seguridad nuevas aparecen en ambos casos. `go build`, `go vet`, `go build -tags=integration` y `go test ./...` limpios.

**Nota sobre el punto 14 (2026-08-09) — decisión de arquitectura consultada antes de tocar código.** Lograr revocación real de sesión (no solo borrar el token del lado del cliente) requiere que el JWT deje de ser 100% autocontenido: hace falta una consulta a BD en cada request autenticado para confirmar que la sesión no fue revocada. Esto choca directamente con una decisión de diseño ya documentada en el propio código (`security/domain/repository.go`, comentario en `GetRole`: *"el rol se incrusta en el JWT... así el RBAC del lado del servidor no necesita una consulta a BD en cada request"*). Se preguntó explícitamente antes de implementar; se confirmó proceder con la opción "más profesional" — verificación real contra BD, no un JWT puramente stateless que no puede revocar nada de verdad.

**Diseño implementado — `token_version`:**
- Nueva columna `security.users.token_version` (migración `000002_token_version`, no se editó la `000001` ya aplicada en producción — production tiene datos reales, ver Fase 0).
- El JWT lleva su propio `token_version` como claim (`"tv"`). `domain.TokenService.Issue`/`Verify` ahora reciben/devuelven ese valor — sigue siendo pura criptografía, sin acceso a BD.
- Nuevo `security/application.SessionVerifier` (implementa `shared/tenant.Verifier`, que ahora recibe `ctx`): verifica la firma/expiración del JWT vía `TokenService`, y ADEMÁS carga el usuario real y compara `token_version` — si no coincide, la sesión se trata como inválida aunque el JWT siga siendo válido criptográficamente. Es la pieza que reemplazó a `jwtSvc` directamente como `tenant.Verifier` en `main.go`.
- `Repository.IncrementTokenVersion` revoca todas las sesiones del usuario de una vez (no hay tabla de sesiones por dispositivo/token individual todavía, así que "cerrar esta sesión" y "cerrar sesión en todos lados" son, por ahora, la misma operación — documentado como limitación conocida, no como descuido).
- **Logout real**: nuevo `LogoutUseCase` + `POST /api/v1/auth/logout` (protegido) → incrementa `token_version`. Frontend (`AuthContext.logout`) ahora llama este endpoint (best-effort, no bloquea el logout local si falla) antes de limpiar `localStorage`.
- **Cambio de contraseña invalida sesiones**: `ChangePasswordUseCase.Execute` incrementa `token_version` tras actualizar la contraseña — pero para no dejar al propio usuario deslogueado de su sesión actual (ya demostró su identidad dos veces: contraseña actual + nueva), re-emite un token fresco con el `token_version` ya incrementado y lo devuelve en la respuesta. Solo las OTRAS sesiones (otros dispositivos) quedan inválidas de inmediato. Frontend actualizado para guardar ese token nuevo.
- **Rate limiting de login**: nuevo `LoginRateLimiter` en memoria (no Redis/BD — a esta escala un mapa protegido por mutex es suficiente y no suma infraestructura nueva), 5 intentos fallidos en 15 minutos bloquean el correo por 15 minutos. Nuevo `domain.ErrTooManyAttempts` → HTTP 429. Limitación conocida y documentada: el contador vive en el proceso (se reinicia si el servidor reinicia; no se comparte si algún día hay más de una réplica).

**Verificado en vivo, no solo compilación** — flujo completo contra el servidor real:
1. Login → token con `"tv":0`. `/auth/me` con ese token → 200.
2. `POST /auth/logout` → 204. **El mismo token, reusado después del logout → 401** (antes de este cambio hubiera seguido dando 200 hasta su expiración de 24h).
3. Rate limiting: 5 intentos con contraseña incorrecta → `401` cada uno; 6º intento → `429`. Se confirmó además que un intento con la contraseña **correcta** mientras la cuenta está bloqueada también da `429` (no `200`) — necesario para que el bloqueo no sirva de oráculo para detectar cuándo se acertó la contraseña.

`go build`, `go vet`, `go build -tags=integration`, `go test ./...` y `npm run build` limpios.

**Nota sobre el punto 15 (2026-08-09).** Las 3 violaciones tenían formas distintas y se resolvieron con el patrón que mejor calzaba en cada caso, no uno solo genérico:

- **`company` → `security.user_companies`** (`ListByUserID`, un subquery `WHERE id IN (SELECT company_id FROM security.user_companies ...)`): resuelto por **composición directa vía puerto estructural**, sin adaptador. `security.Repository.ListCompanyIDs` ya existía (la usa `LoginUseCase`) y devuelve `[]uuid.UUID` — un tipo neutro que no exige traducción. Se definió `company/application.MembershipReader` (interfaz mínima con ese único método) y `GetUseCase.ListByUserID` ahora hace `ids, _ := members.ListCompanyIDs(...)` seguido de `repo.ListByIDs(ctx, ids)` (que también ya existía). `securityRepo` se pasa directo como `MembershipReader` en `main.go` — Go lo acepta por tipado estructural, sin ningún archivo de adaptador, igual que ya hacía `MembershipLinker` para `IsSuperAdmin`/`AddCompany`. Se eliminó `ListByUserID` de `company/domain.Repository` y de la implementación postgres — la query cross-schema desapareció, no se reemplazó por otra.

- **`audit` → `security.users`** (`LEFT JOIN security.users u ON u.id = ae.user_id`, para mostrar `user_email`/`user_name` en cada evento): este caso sí necesitaba traducción real (el repositorio de `security` devuelve su propio `domain.User`, con muchos más campos de los que `audit` necesita), así que se construyó el adaptador completo: `security.Repository` ganó `GetByIDs(ctx, ids) ([]User, error)` (lookup por lote, mismo patrón que `company.ListByIDs`); `audit/application` ganó `UserLookupPort` + tipo propio `UserInfo` (solo `ID`/`Email`/`Name`); nuevo `audit/infrastructure/security.Adapter` traduce `security.domain.User` → `audit/application.UserInfo`. El repositorio postgres de `audit` ahora solo consulta `audit.events` (sin JOIN); el enriquecimiento por usuario se mueve a `UseCase.List` en la capa de aplicación: junta los `user_id` distintos de la página de eventos, pide esos usuarios en un solo `GetByIDs` (no N+1), y rellena `UserEmail`/`UserName` en Go. Si ese lookup falla, se listan los eventos igual (sin nombre) en vez de romper el listado completo.

- **`stats` → `electronic.documents`** (3 queries de agregación con `FILTER`/`GROUP BY`/zona horaria Bogotá para el dashboard): este era el caso más grande — mover la agregación a Go (traer todas las filas y sumar en memoria) habría sido mucho menos eficiente que dejar que Postgres agregue. Primero se movieron las queries (tal cual, sin reescribirlas) al módulo dueño real del schema: nuevo `electronic/domain.BillingStats`/`PeriodStats`/`TypeStats`/`MonthSeries` + `electronic/domain.BillingStatsRepository`, con `electronic/infrastructure/persistence/postgres.BillingStatsRepository` implementándolo, y un adaptador `stats/infrastructure/electronic.Adapter` traduciendo de vuelta a `stats/domain.BillingStats`.

**Corrección el mismo día: se revirtió el adaptador y se eliminó el módulo `stats/` por completo.** Al revisar por qué el punto 15 había requerido tanto código nuevo solo para este caso, se encontró que `docs/Diseno_ERP_Go_Arquitectura_Hexagonal.md:35,69-80` documenta `stats/` como un módulo transversal de solo lectura diseñado **a propósito** para saltarse el límite de módulo ("queries directas sobre los schemas correspondientes, sin importar módulos Go") — no era la violación real que el resto sí eran. Además, la extensión prevista en ese documento (`/stats/sales`, `/stats/inventory`, `/stats/payroll`) nunca se construyó: cada módulo terminó con su propio dashboard client-side (`SalesDashboardPage.tsx`, `PurchaseDashboardPage.tsx`, `InventoryDashboardPage.tsx`, `AccountingDashboardPage.tsx`) que calcula sus métricas en el frontend reusando sus propios endpoints, sin tocar `stats` para nada. `stats/` había quedado congelado como un passthrough de una sola tabla (`electronic.documents`) con un solo consumidor (`GET /stats/billing`, el dashboard general) desde julio de 2026.

Mantener `stats/` como módulo independiente con adaptador de traducción era sobreingeniería sobre un límite que nadie más cruzaba. Se eliminó el módulo completo (`domain`, `application`, `infrastructure`, `interfaces`).

**Tercera vuelta, mismo día: tampoco se dejó el endpoint en `electronic`.** Dejar `GET /api/v1/stats/billing` viviendo en `electronic/interfaces/http` (con su propio `domain.BillingStats` + `BillingStatsRepository` en SQL) reproducía el mismo problema un nivel más abajo: `electronic` quedaba como el único módulo de negocio con un endpoint de backend dedicado a estadísticas, mientras `sales`, `purchase`, `inventory` y `accounting` siempre calcularon las suyas 100% en el frontend (`SalesDashboardPage.tsx` y equivalentes, sin ningún endpoint de agregación, solo sus listados CRUD de siempre). Se eliminó también esa maquinaria de `electronic` (`domain/billing_stats.go`, `infrastructure/persistence/postgres/billing_stats_repository.go`, `application/get_billing_stats.go`, ruta y handler) y se reemplazó por `frontend/src/lib/electronicStats.ts` (`computeBillingStats`), que calcula lo mismo en el cliente a partir de `GET /electronic/documents` — el mismo endpoint CRUD que ya existía, ahora estandarizado con los demás módulos.

De paso se corrigió un bug real descubierto en el camino: `handleListDocuments` ignoraba `status`/`from`/`to`/`search`/`offset` del query string (el frontend ya los mandaba, `domain.ListFilter` y el repositorio postgres ya los soportaban de punta a punta) y forzaba `Limit: 50` fijo — solo faltaba conectarlos en el handler. Se corrigió sin cambiar el comportamiento por defecto (sigue defaulteando a 50, tope 200).

Con esto, `electronic` ganó su propia página de aterrizaje de módulo (`ElectronicDashboardPage.tsx`, ruta `/documents`, sidebar actualizado), al mismo nivel que `/sales`, `/purchases`, `/inventory`, `/accounting` — un hueco real que quedó expuesto al revisar por qué `stats/` existía. El dashboard general (`/`) conserva su grid de widgets personalizable (arrastrar/redimensionar/ocultar, ya construido y genuinamente transversal, no cuestionado) pero ahora se alimenta del mismo cálculo client-side en vez de un endpoint de backend; sigue mostrando solo datos de `electronic` — extenderlo a tarjetas de otros módulos queda pendiente como trabajo futuro, no resuelto en este punto. No se movió nada a `shared/` porque la lógica tiene reglas de negocio propias de `electronic` (estados DIAN, tipos de documento). Detalle completo en `01-arquitectura.md` y `02-base-de-datos.md`.

**Verificado en vivo los 3 casos**, no solo compilación: se creó una empresa nueva con el superadmin, se confirmó que `GET /companies` la lista (composición `ListCompanyIDs`+`ListByIDs`), que `GET /audit-events` sigue devolviendo `user_email`/`user_name` correctos para los eventos de login/selección de empresa (enriquecidos en Go, no por JOIN SQL), y que `GET /stats/billing` ya no existe (`404`, esperado — la agregación se movió al cliente). Se probó también `GET /electronic/documents` con los filtros recién conectados (`status`, `from`) respondiendo `200`. `go build`, `go vet`, `go build -tags=integration`, `go test ./...` y `npm run build` (frontend) limpios.

**Nota sobre el punto 16 (2026-08-09).** Los 4 ajustes de BD se integraron directamente en las migraciones `000001` existentes de cada módulo (sin `ALTER` separado, siguiendo la convención de este proyecto mientras no haya datos reales — ver [[feedback_sql_schema_conventions]]):

- **`.down.sql` faltantes**: `000001_electronic.down.sql`, `000001_hr.down.sql`, `000001_payroll.down.sql`, `000001_saas.down.sql`, mismo patrón `DROP SCHEMA IF EXISTS <modulo> CASCADE;` que los otros 10 módulos.
- **`UNIQUE(company_id, number)`**: `CREATE UNIQUE INDEX ... WHERE number != ''` en `sales.sales` y `purchase.orders` (condicional, para no romper los borradores que usan `number=''` por defecto).
- **Índices en FKs de `electronic.documents`**: `numbering_range_id` (índice normal), `customer_id` y `supplier_id` (parciales, `WHERE ... IS NOT NULL`, ya que ambas columnas son nullable según el tipo de documento).
- **Pool de conexiones**: `MaxConns=20`, `MinConns=2`, `MaxConnIdleTime=5m` explícitos en `pgxpool.Config` (`mustOpenDB`, `erp/cmd/server/main.go`).

Como `sales`, `purchase` y `electronic` ya tenían su migración `000001` aplicada localmente, se dropearon esos 3 schemas (+ sus tablas `*_schema_migrations`) y se dejó que `golang-migrate` los recreara desde cero con los cambios ya integrados — sin datos reales que perder en este entorno. `hr`/`payroll`/`saas` no necesitaron ese paso (solo se les agregó el `.down.sql`, que `golang-migrate` no ejecuta en un arranque normal). Verificado en vivo: los 14 módulos migran limpio (`migrate OK` en logs), `\d sales.sales`/`\d purchase.orders`/`\d electronic.documents` confirman los nuevos índices/constraints en Postgres, y `go test -tags=integration` (schema tests) pasa en los 6 módulos tocados. `go build`, `go vet`, `go build -tags=integration` y `go test ./...` limpios.

---

**Nota sobre el punto 17 (2026-08-09).** Nuevo `.github/workflows/ci.yml` con dos jobs, corriendo en cada push y pull request:

- **`backend`**: Postgres 18 como servicio del job. `go vet` en `erp` y `cofacture`. `go build ./...` y `go build -tags=integration ./...` en `erp` (`cofacture` no usa ese tag). Un paso intermedio compila y levanta el binario real del servidor contra el Postgres del job (con `DATABASE_URL`/`JWT_SECRET`/`ENCRYPTION_KEY`/`CORS_ALLOWED_ORIGINS` de CI, sin superadmin ni SMTP configurados) y espera a `GET /health` con reintentos — esto deja aplicadas las migraciones de los 14 módulos sin duplicar esa lógica de arranque en el workflow, ya que `main()` migra secuencialmente antes de escuchar el puerto. Si el server no responde a tiempo, el step falla explícitamente con el log completo en la salida, en vez de dejar que los tests de esquema fallen después con un error de tabla inexistente menos claro. Con el schema ya aplicado, corre `go test ./... -tags=integration` en `erp` (schema tests + tests de aplicación) y `go test ./...` en `cofacture`. Por último, `govulncheck ./...` en ambos módulos Go — verificado hoy en 0 (ambos módulos están libres de vulnerabilidades explotables por su propio código; solo hay 1 vulnerabilidad en cada uno en una dependencia que el código no llega a usar), así que se dejó **bloqueante**.
- **`frontend`**: `npm ci`, `npm run lint` (oxlint — hoy solo warnings, exit 0, así que también bloqueante), `npm run build` (`tsc -b` + `vite build`), y `npm audit --audit-level=high` **no bloqueante** (`continue-on-error: true`): hoy ya existen vulnerabilidades altas conocidas y sin corregir (`nanoid`, `postcss`, transitivas) — es justamente el punto 20, todavía pendiente. Bloquear el CI con `npm audit` antes de resolver el punto 20 habría dejado el pipeline en rojo desde el primer push por algo ya conocido y fuera del alcance de este punto; se deja reportando en cada corrida y queda anotado que debe volverse bloqueante (quitar `continue-on-error`) en cuanto el punto 20 se resuelva.

No se creó `Makefile` ni se cambiaron instrucciones de desarrollo local (recomendación cosmética del informe 07, no bloqueante) — el comando estándar para tests de esquema locales sigue siendo `go test ./... -tags=integration` con `DATABASE_URL` exportada a mano.

**Verificado**, no solo redactado: se replicó localmente la lógica exacta del step de migración/health-check contra una base de datos Postgres temporal (simulando el servicio del job) — migró los 14 módulos limpio, `/health` respondió `200` tras el arranque, y `go test ./... -tags=integration` pasó completo contra ese mismo schema recién creado. `govulncheck` se corrió de verdad en `erp` y `cofacture` (0 vulnerabilidades explotables en ambos). `npm run lint`/`npm run build` se corrieron localmente y pasan. El workflow en sí no se pudo ejecutar dentro de GitHub Actions desde este entorno (requiere push); la verificación fue reproducir cada paso exacto del YAML de forma local antes de confiarlo al CI real.

**⚠️ Corrección (2026-08-10) — la primera corrida real en GitHub Actions falló.** El usuario hizo push y el job `backend` falló en el primer step (`go vet (erp)`) con `no required module provides package github.com/diegofxm/cofacture/...`. Causa raíz: `erp/go.mod` no declara `cofacture` como dependencia (ni `require` ni `replace`) a propósito — la conexión entre ambos módulos solo existe vía `go.work` en la raíz del repo, y `go.work`/`go.work.sum` están en `.gitignore` (comentario propio: "puente de desarrollo en paralelo... es local del entorno de quien desarrolla"). El checkout de GitHub Actions es un clon limpio del historial de git — nunca tuvo ese archivo, así que `erp` quedaba huérfano de `cofacture` en CI aunque localmente todo compilara perfecto.

Esto expuso un hueco real en la verificación original: **nunca se probó un checkout limpio**, solo se reprodujeron los pasos dentro de este mismo entorno de desarrollo, que siempre tuvo `go.work` presente — el mismo punto ciego, replicado. Se corrigió agregando un step nuevo (`crear go.work (no versionado, ver .gitignore)`) que regenera el archivo con `printf` (evitando cualquier ambigüedad de indentación de un heredoc dentro de un bloque YAML) antes de los steps de Go. Esta vez la verificación sí cubrió el escenario real: se usó `git worktree add` para crear un checkout limpio de `HEAD` (sin archivos ignorados ni sin trackear, igual que un clon de GitHub Actions), se reprodujo el fallo original ahí, y se confirmó que el `go.work` generado por el `printf` exacto del workflow es **idéntico byte a byte** al `go.work` real, y que con ese archivo `go vet`/`go build` (con y sin `-tags=integration`) pasan limpio en `erp` y `cofacture` dentro de ese checkout limpio.

**⚠️ Segunda corrección (2026-08-10), mismo push — `govulncheck` falló con un CVE real del stdlib.** Con el `go.work` arreglado, el job avanzó hasta `govulncheck (erp)` y falló ahí: `GO-2026-5856` ("Invoking Encrypted Client Hello privacy leak", `crypto/tls`), con rastros de llamada reales (`http.ListenAndServe` → `tls.Conn.HandshakeContext`, `cryptutil.Encrypt` → `tls.Conn.Read`, el generador CSV, el envío SMTP) — no un falso positivo, `govulncheck` lo marcó como código alcanzable de verdad. Causa: el workflow fijaba `go-version: "1.26.4"` en `actions/setup-go@v5` — exactamente la versión con el bug (corregido en `1.26.5`). La máquina de desarrollo local ya tenía Go 1.26.5 instalado, así que cada vez que se corrió `govulncheck` "en vivo" en este entorno durante los puntos 06/17, el bug ya estaba parchado en el toolchain usado y nunca apareció — el mismo patrón del error anterior: verificar en un entorno que no coincide exactamente con el de CI oculta el problema en vez de confirmarlo.

Fix: `go-version: "1.26"` en vez de `"1.26.4"` fijo — instala siempre el último parche de la serie 1.26.x (ni `erp/go.mod` ni `cofacture/go.mod` tienen directiva `toolchain`, solo `go 1.26.4` como mínimo, así que un parche más nuevo es compatible sin tocar esos archivos). Verificado corriendo `govulncheck` de verdad con Go 1.26.5 instalado en `erp` y `cofacture`: 0 vulnerabilidades en ambos. Lo único que no se pudo verificar desde este entorno es que `actions/setup-go@v5` resuelva `"1.26"` como "último parche" dentro del runner real de GitHub — es el comportamiento documentado de esa acción, pero la confirmación definitiva llega con el próximo push.

---

**Nota sobre el punto 18 (2026-08-10).**

1. **Healthcheck real.** `GET /health` en `erp/cmd/server/main.go` ahora hace `pool.Ping(ctx)` con timeout de 3s: `503` + `{"status":"error",...}` si la base no responde, `200` solo si el pool está sano. Verificado en vivo el camino feliz (`200` con Postgres real arriba); el camino de falla no se forzó a propósito para no interrumpir el Postgres local de desarrollo, pero la lógica es la API estándar de `pgxpool.Pool.Ping` sin ninguna rama exótica.

2. **Discrepancia de infraestructura — resuelta, y era peor de lo que decía la auditoría original.** Ni Railway ni Neon son lo que corre hoy. Se confirmó con el usuario el estado real:
   - **Servidor**: una VPS auto-administrada, con un panel de despliegue propio del usuario ("dpanel"): el flujo real es compilar el binario localmente y subirlo desde un selector de archivo en el panel; dpanel lo publica en `/srv/dpanel/apps/api-cofacture` (patrón release + symlink `current`) y reinicia el proceso, gestionado por **systemd** (`app-api-cofacture.service`). No hay `git pull` en el servidor.
   - **Base de datos**: **alwaysdata.net**, no Neon — Neon se evaluó en paralelo pero nunca se adoptó oficialmente (sigue solo como ejemplo en `erp/.env.example`).
   - Es un entorno de **pruebas**, no una producción confirmada con clientes reales — la corrección documental refleja eso explícitamente, no lo presenta como "la producción".
   - `docs/apidian-architecture.md:2727-2742` (y el paso 1 de la secuencia de implementación más abajo en el mismo documento) quedaron anotados con esta corrección, sin borrar el contenido original (queda tachado/marcado, no eliminado, para no perder el rastro de qué se pensaba antes).

3. **Backup — deliberadamente sin resolver.** El usuario confirmó que **no sabe si hay backup real hoy**: no ha verificado si el plan de alwaysdata incluye backups automáticos, y todavía está decidiendo si el backup debería vivir en la misma base de producción o mantenerse separado de la VPS a propósito (defensa en profundidad ante un compromiso del servidor). No se implementó ni se documentó ninguna estrategia de backup como si estuviera resuelta — eso habría sido inventar una respuesta a una decisión que es del usuario, no técnica. Queda anotado como pendiente explícito, no como "hecho".

4. **Script de deploy y unit de systemd — versionados tal cual son, no inventados.** `docs/deploy/app-api-cofacture.service`: contenido real capturado con `sudo systemctl cat app-api-cofacture` (compartido por el usuario), versionado exactamente igual, con una nota de que dpanel ya lo administra y no hace falta reinstalarlo desde el repo. `docs/deploy/build-release.ps1`: como el flujo real es "compilar localmente → subir binario ya compilado", no "pull + build + restart en el servidor" (que fue la redacción original de la recomendación, pero no aplica a este flujo), el script que tiene sentido versionar es el de compilación: cross-compila `erp/cmd/server` a `linux/amd64` con `CGO_ENABLED=0` (verificado: produce un ELF estático válido) y deja el binario nombrado `bin` en la raíz del repo — el nombre exacto que espera `ExecStart=/srv/dpanel/apps/api-cofacture/current/bin` — listo para subir al selector de archivo de dpanel. Se agregó `/bin` a `.gitignore` para que ese binario compilado (27MB) nunca se comitee por accidente. Verificado ejecutando el script de punta a punta: genera el binario, es un ELF Linux x86-64 válido, se limpió después de la prueba.

---

**Nota sobre el punto 19 (2026-08-10).** Flujo mínimo de Habeas Data (Ley 1581 de 2005) en `thirdparty`:

- **Alcance correcto: solo persona natural.** `Party` gana `HabeasDataConsent`/`HabeasDataConsentAt`, integrados en la migración `000001_thirdparty.up.sql` (sin `ALTER`, mismo criterio que puntos anteriores). La lógica en `applyShared` (`thirdparty/application/create.go`) fuerza estos campos a `false`/`nil` cuando `EntityTypeCode != "2"` — una persona jurídica no es titular bajo esta ley, así que el consentimiento se ignora silenciosamente en vez de guardarse sin sentido. La fecha se estampa una sola vez (la primera vez que se marca) y se conserva en ediciones posteriores; al desmarcar, se limpia. Cubierto con 3 tests nuevos en `create_test.go` (estampado, ignorado en jurídica, preservación/revocación en edición) — los 4 tests de aplicación de `thirdparty` (los únicos que ya existían en el módulo, junto con `accounting`/`sales`/`shared/nit`) siguen pasando.
- **Exportación (derecho de Acceso)**: nuevo `GET /api/v1/customers/{id}/export` y `/suppliers/{id}/export`, mismo `GetUseCase.ByID` ya existente envuelto en un sobre `{exported_at, titular}`. Cada exportación se audita como `<role>.data_exported` — distinto de una simple consulta `GET /{id}`, para poder demostrar cuándo se atendió una solicitud real, no solo que alguien vio el registro. Frontend: botón de descarga (ícono, visible solo si `entity_type_code=="2"`) en `/customers` y `/suppliers`, descarga un `.json` client-side.
- **Procedimiento ARCO**: `docs/habeas-data-arco.md`, con disclaimer explícito de que no reemplaza asesoría legal. Cubre quién es titular, qué existe hoy, cómo atender cada derecho (acceso/rectificación/cancelación/oposición) manualmente, plazos de ley (10/15 días hábiles, a confirmar con abogado), y limitaciones deliberadas: la Cancelación no purga documentos históricos en `electronic`/`sales`/`purchase` (tensiona con la obligación de conservación documental de la DIAN, no es un descuido), y el export no agrega automáticamente esos documentos (queda como paso manual si se necesita).

**Verificado en vivo**, no solo compilación: se creó un cliente persona natural con consentimiento (quedó estampado `habeas_data_consent_at`), se exportaron sus datos (`200`, JSON completo) y se confirmó en `GET /audit-events` que quedaron los dos eventos (`customer.created` y `customer.data_exported`) con `user_email`/`user_name` resueltos. Se editó el mismo cliente desmarcando el consentimiento (se limpió `habeas_data_consent_at`). Se creó un proveedor persona jurídica con `habeas_data_consent:true` en el request y se confirmó que el backend lo ignoró (`false`/`null` en la respuesta). Se dropeó y re-migró el schema `thirdparty` localmente para aplicar las columnas nuevas. `go build`, `go vet`, `go build -tags=integration`, `go test ./...` y `npm run build`/`npm run lint` (frontend) limpios.

---

**Nota sobre el punto 20 (2026-08-10) — cierra la Fase 2.** `npm audit fix` (sin `--force`) resolvió las 3 vulnerabilidades altas de una sola vez: `react-router` 7.18.0→7.18.2 (el fix ya cabía dentro del rango `^7.18.0` declarado en `package.json` — no hizo falta editarlo), y `postcss`/`nanoid` (transitivas de `vite`) a `8.5.26`/`3.3.18`. `npm audit` quedó en 0 vulnerabilidades. Único archivo con diff real: `frontend/package-lock.json` (10 líneas). Se aprovechó para cumplir lo que el propio `.github/workflows/ci.yml` (punto 17) dejó anotado: el step `npm audit --audit-level=high` pasó de `continue-on-error: true` a **bloqueante**, ahora que no hay nada pendiente que lo hiciera fallar desde el primer push.

**Verificado**: `npm run build`, `npm run lint` y `npm audit --audit-level=high` (ahora en `0`) corridos localmente tras la actualización, todos limpios. No se tocó ningún código de la aplicación, solo dependencias — sin necesidad de pruebas funcionales adicionales más allá del build/lint que ya cubren errores de compatibilidad de tipos.

Con esto se recorrieron los 13 puntos de la **Fase 2** (08-20). No todos quedaron 100% resueltos a propósito: el punto 08 sigue 🟡 parcial (el barrido sistemático de RBAC en todo módulo, más allá de los 3 ejemplos ya resueltos, queda pendiente) y el punto 18 sigue 🟡 parcial (la estrategia de backup real es una decisión del usuario todavía sin tomar, no algo que se pueda completar técnicamente sin esa decisión). El resto (09-17, 19, 20) está ✅ resuelto y verificado en vivo.

---

## Fase 3 — Frontend: confiabilidad de sesión y accesibilidad

Con el backend ya autorizando y auditando correctamente, el frontend deja de tener que compensar ocultando botones como única defensa, y puede enfocarse en su propia deuda.

| # | Tarea | Depende de | Esfuerzo | Informe |
|---|---|---|---|---|
| **21** | Interceptor global de 401 en `apiClient.ts` con logout + redirección automática (hoy cada página muestra un error genérico). | ✅ **Resuelto** (ver nota) | 14 (necesita que exista logout server-side) | Medio | 05 |
| **22** | Timeout de red (`AbortController`) en `fetch()` dentro de `request()`/`getBlob()`. | ✅ **Resuelto** (ver nota) | — | Bajo | 05 |
| **23** | Hook compartido `useApiResource` (loading/error/cancelado) para reemplazar el patrón duplicado y con variaciones entre páginas. | ✅ **Resuelto** (ver nota — adopción parcial a propósito) | 21, 22 (se construye sobre el cliente ya corregido) | Medio | 05 |
| **24** | Accesibilidad: `aria-label` en botones icon-only e inputs de búsqueda crudos, semántica ARIA (`role="combobox"`, etc.) en `Combobox.tsx`. | ✅ **Resuelto** (ver nota — barrido completo, no parcial) | — | Bajo-Medio | 05 |
| **25** | Smoke tests de frontend (Vitest + Testing Library) sobre los flujos críticos: login, confirmación de venta/factura. | ✅ **Resuelto** (ver nota — cierra la Fase 3) | 21-24 (para testear el comportamiento ya corregido) | Medio | 07 |

**Nota sobre el punto 21 (2026-08-10).** Interceptor global de 401 en `frontend/src/lib/apiClient.ts`:

- **Mecanismo**: `setSessionExpiredHandler(handler)` registra un callback que `request()`/`getBlob()` invocan cuando la respuesta es 401. `apiClient.ts` no puede importar `AuthContext.tsx` directamente (dependencia circular, `AuthContext` ya importa `apiClient`), así que la relación se invierte con este callback — mismo patrón que ya usa el proyecto para desacoplar módulos (`AuditLogger` local en cada handler HTTP del backend).
- **No todo 401 significa "sesión inválida".** Investigando el backend se encontró que `security` usa 401 también para `ErrInvalidPassword` (login) y `ErrCurrentPasswordIncorrect` (cambiar contraseña) — respuestas de negocio normales, con el usuario todavía autenticado en el segundo caso. Se excluyeron por ruta (`/auth/login`, `/auth/password`, `/auth/logout` — este último para no crear un loop entre `logout()` y el propio interceptor) en vez de disparar el logout automático a ciegas en cualquier 401, que habría desconectado a alguien por un simple typo al cambiar su contraseña.
- **Sin navegación explícita.** `ProtectedRoute.tsx` ya redirige a `/login` reactivamente en cuanto `isAuthenticated` (derivado de `user !== null`) pasa a `false` — no hace falta llamar `useNavigate()` desde el interceptor, que además no podría: `AuthProvider` está fuera del `<BrowserRouter>` en `App.tsx`.
- **Guard contra ráfagas**: un `useRef` en `AuthContext.tsx` evita que varios 401 casi simultáneos disparen `logout()` más de una vez; se reinicia en cada login/verificación de sesión exitosa.
- **Limitación aceptada a propósito**: si la sesión se revoca justo mientras el usuario está en el formulario de cambiar contraseña, no se le desconecta automáticamente ahí — sí en la siguiente petición a cualquier otro endpoint. El backend no distingue "contraseña actual incorrecta" de "sesión revocada" con un código de error propio, solo con el texto del mensaje (`"error de contraseña actual"` vs `"no autenticado"`) — depender de ese texto para decidir sería frágil, así que se prefirió el falso negativo raro sobre el falso positivo común (typo → desconexión).

**Verificado en vivo** contra el servidor real, no solo compilación: login normal, luego `POST /auth/logout` para revocar la sesión (incrementa `token_version`), y con el **mismo token viejo**: `GET /customers` → `401 {"error":"empresa activa requerida"}` (fuera de la lista de exención — dispararía el interceptor), `PUT /auth/password` → `401 {"error":"no autenticado"}` (dentro de la lista de exención — no dispara, comportamiento esperado). `npm run build` y `npm run lint` limpios. La interacción real en navegador (clic → 401 → redirección visible) queda pendiente de que el usuario la confirme visualmente, como el resto de cambios de frontend en esta sesión — no hay Playwright/chromium-cli en este entorno.

---

**Nota sobre el punto 22 (2026-08-10).** `fetchWithTimeout()` en `apiClient.ts` envuelve `request()`/`getBlob()` con `AbortSignal.timeout(20_000)`. Al vencer los 20s lanza una clase propia `TimeoutError`.

**Decisión que se apartó de la redacción original de la recomendación**: el informe 05 sugería "mapear el abort a un `ApiError`" — se decidió **no** hacerlo así. `TimeoutError` no extiende `ApiError` a propósito: un timeout significa que nunca hubo respuesta del servidor (falla de conectividad), no que el servidor respondió con un error de negocio real. El resto del código ya distingue estas dos categorías — `AuthContext.verifySession()` solo activa `connectionError` (el banner "no se pudo conectar" con botón "Reintentar", `ConnectionBanner.tsx`) cuando el error capturado **no** es un `ApiError`. Si el timeout se hubiera envuelto como `ApiError`, un backend colgado durante la verificación de sesión al abrir la app se habría mostrado como una desconexión de sesión en vez de como un problema de conectividad con reintento — peor experiencia que la que ya existía. Envolverlo como una clase separada deja que el timeout caiga naturalmente en el mismo camino que ya existe para "sin red", reusando esa UX en vez de duplicar un concepto nuevo.

**Verificado el mecanismo real**, no solo la compilación: como `apiClient.ts` es un módulo de navegador (no se puede importar tal cual en un script de Node por las dependencias de Vite/`import.meta.env`), se replicó la función `fetchWithTimeout` exacta en un script standalone de Node (que sí tiene `fetch`/`AbortSignal` nativos) contra dos servidores HTTP reales: uno que nunca responde (cuelga la conexión a propósito) y uno que responde normal. Con el mismo timeout configurado en 800ms para la prueba: contra el servidor colgado, lanzó `TimeoutError` a los ~811ms — no se quedó esperando para siempre; contra el servidor normal, la respuesta pasó sin ninguna interferencia. `npm run build` y `npm run lint` limpios. La experiencia visual real (spinner + mensaje en una página real del navegador) queda pendiente de confirmación del usuario, mismo motivo que el punto 21 (sin Playwright/chromium-cli en este entorno).

---

**Nota sobre el punto 23 (2026-08-10) — adopción parcial a propósito.** Nuevo `frontend/src/hooks/useApiResource.ts`: centraliza `loading`/`error`/protección contra `setState` post-desmontaje (mismo patrón `cancelled` que ya usaban `DashboardPage.tsx`/`useMyPlan.ts`, ahora generalizado), con `refresh()` para revalidar sin duplicar la lógica de fetch, y una opción `enabled` para no disparar la petición mientras falte una dependencia (ej. la empresa activa todavía no está lista).

**Se migraron exactamente las páginas que el propio hallazgo nombró como ejemplo del problema, ni una más:**
- `DashboardPage.tsx`: el fetch de documentos ahora vive en el hook; `stats` y `recentDocs` se derivan con `useMemo` del mismo dato (`documents`), reemplazando los dos `setState` separados que antes hacía el `.then()` manual.
- `CustomersPage.tsx`/`SuppliersPage.tsx` (gemelas): además de adoptar el hook, se separó el error de "no cargó la lista" (ahora lo maneja el hook, `loadError`) del error de "no se pudo guardar" (sigue siendo estado local de la página, `saveError`) — antes ambos compartían una sola variable `error`, mezclando dos causas distintas en el mismo mensaje.

**Deliberadamente no se migraron las ~35+ páginas restantes** (sales, purchase, inventory, accounting, editores de documentos, etc.). Migrarlas todas habría sido un refactor masivo tocando prácticamente cada página del frontend, muy por encima del esfuerzo "Medio" estimado para este punto y con un riesgo de regresión desproporcionado para lo que pedía la recomendación — que, en su propia redacción, apuntaba a que **las páginas nuevas** partieran de un único patrón, no a reescribir las existentes de una sola vez. El hook queda disponible, ya probado en 3 páginas reales de producción; el resto de páginas se migra de forma incremental cuando se toquen por otro motivo, no como trabajo de este punto.

**Verificado**: `go` no aplica (cambio 100% frontend). `npm run build` (`tsc -b` + `vite build`) y `npm run lint` limpios en las 4 páginas/hooks tocados, sin warnings nuevos más allá de la línea base ya existente del proyecto. No se agregó Vitest/Testing Library para probar el hook de forma aislada — eso es exactamente el alcance del punto 25 (todavía pendiente), no de este punto; la verificación aquí se apoyó en que el patrón `cancelled`/`loading`/`error` que replica ya estaba probado en producción (`DashboardPage.tsx` original, `useMyPlan.ts`), y en que las 3 páginas migradas compilan y buildean limpio con el comportamiento visual equivalente razonado explícitamente caso por caso (ver diff).

---

**Nota sobre el punto 24 (2026-08-10) — barrido completo, a diferencia del punto 23.** A diferencia del hook de loading/error (riesgo estructural real), agregar `aria-label` es 100% aditivo — no cambia comportamiento ni estado, solo metadata de accesibilidad — así que aquí sí se justificaba un barrido completo en vez de una adopción parcial.

- **Combobox.tsx**: patrón WAI-ARIA 1.2 "combobox con listbox popup" completo (`role="combobox"`/`aria-expanded`/`aria-controls`/`aria-autocomplete`/`aria-activedescendant` en el input, `role="listbox"`/`role="option"`/`aria-selected` en el desplegable), con IDs estables vía `useId()`. Nueva prop `ariaLabel` para los pocos usos sin `label` visible (útil en celdas de tabla). El foco del DOM sigue en el `<input>` todo el tiempo — `aria-activedescendant` solo "anuncia" cuál opción está resaltada, sin moverlo de verdad, mismo mecanismo que ya usaba la navegación por teclado existente.
- **Botones icon-only**: se buscó sistemáticamente con grep todo `<button>`/`<Link>` con `title=` sin `aria-label` acompañante, no solo los dos ejemplos que citaba el hallazgo — 16 archivos en total: `Navbar.tsx`, `NotificationBell.tsx`, `ThemeToggle.tsx`, `AdminPage.tsx`, `CustomersPage.tsx`, `SuppliersPage.tsx`, `ProductsPage.tsx`, `WarehousesPage.tsx`, `NumberingRangesPanel.tsx`, `PublicRegistrationPanel.tsx`, `LineItemsEditor.tsx`, `SalesLineItemsEditor.tsx`, `PaymentMeansEditor.tsx`, `WithholdingTaxesEditor.tsx`, y (hallazgo nuevo en el camino) `AccountingJournalEditorPage.tsx`. Donde tenía sentido (filas de tabla con varias entidades del mismo tipo), el `aria-label` incluye el nombre/número de fila (`"Editar Juan Pérez"`, `"Débito línea 3"`) en vez de un genérico `"Editar"` que no distingue entre filas para un lector de pantalla. El menú de cuenta y la campana de notificaciones ganaron además `aria-haspopup`/`aria-expanded` (patrón de disclosure), no solo el `aria-label`.
- **Hallazgo no buscado, encontrado en el camino**: la grilla de líneas contables en `AccountingJournalEditorPage.tsx` (4 columnas de formulario por fila: Cuenta/Descripción/Débito/Crédito, más el botón eliminar) no tenía ni `scope="col"` en los `<th>` ni nombre accesible en ningún control de ninguna fila — el peor caso de todo el barrido, con más controles sin etiquetar que cualquier otro archivo. Se corrigió completo.
- **Inputs de búsqueda crudos**: `aria-label` en los 3 que lo necesitaban (`CustomersPage.tsx`, `SuppliersPage.tsx`, `ProductsPage.tsx`). Se verificó primero que los pares "Desde"/"Hasta" en las páginas de documentos (`InvoicesPage.tsx`, `CreditNotesPage.tsx`, `DebitNotesPage.tsx`, `SupportDocumentsPage.tsx`, `AdjustmentNotesPage.tsx`) y los `Combobox` con `label` (`AccountingBankPage.tsx`, `AccountingBudgetsPage.tsx`, `PurchaseOrderEditorPage.tsx`, `QuoteEditorPage.tsx`, `SaleEditorPage.tsx`, `AdjustStockModal.tsx`, `TransferStockModal.tsx`, etc.) ya estaban correctamente etiquetados — la sospecha inicial de que también les faltaba resultó ser un falso positivo de una búsqueda de contexto insuficiente, corregida antes de tocar nada innecesariamente.

**Verificado**: `npm run build` y `npm run lint` limpios tras el barrido completo (16 archivos), sin warnings nuevos. Verificación puramente estática (grep sistemático + revisión manual de cada coincidencia) dado que es un cambio de atributos HTML sin lógica — no se dispone de un lector de pantalla real ni de herramientas de auditoría axe/Lighthouse en este entorno para confirmar el resultado final con una herramienta automatizada; la corrección sigue el patrón WAI-ARIA documentado, pero la validación con un lector de pantalla real queda pendiente de que el usuario la haga, igual que el resto de verificaciones visuales de esta sesión.

---

**Nota sobre el punto 25 (2026-08-10) — cierra la Fase 3.** Infraestructura de tests de frontend desde cero: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom` como dependencias nuevas; `vitest.config.ts` separado de `vite.config.ts` a propósito (para no arriesgar el tooling de build/dev existente al agregar tests); `src/test/setup.ts` con los matchers de jest-dom y limpieza entre tests (`cleanup()` + `localStorage.clear()`); script `npm test`. Se usó `globals: false` (default) en vez de `globals: true` para no tener que tocar `tsconfig.app.json` — cada test importa `describe`/`it`/`expect`/`vi` explícito desde `"vitest"`.

Los 2 smoke tests pedidos por el punto (login, confirmación de venta/factura) **no son tests aislados con mocks livianos** — montan la página real dentro de su árbol de contexto real (`AuthProvider`, y para la venta también `ConfirmProvider`/`ToastProvider`, todos sin mockear) y solo interceptan `fetch()` por ruta con un router de mocks mínimo. Esto prueba el camino completo página → contexto → `apiClient` → `fetch`, no una versión simplificada que podría quedar desincronizada del comportamiento real:

- `LoginPage.test.tsx`: credenciales correctas → navega al panel principal y persiste la sesión en `localStorage`; credenciales incorrectas → muestra el mensaje de error real del servidor y no navega.
- `SaleEditorPage.test.tsx`: carga una venta en borrador real, hace clic en "Confirmar", **interactúa con el modal de confirmación real** (`ConfirmDialog`, no `window.confirm`) haciendo clic de verdad en sus botones, y verifica que el backend recibió `POST /sales/:id/confirm` (implícito: el router de mocks lanza si se llama una ruta no declarada) y que la UI refleja "Confirmada". Segundo caso: cancelar el diálogo no llama a confirmar y la venta sigue en "Borrador".

**Efecto colateral encontrado escribiendo el test, corregido de una vez**: `ConfirmDialog.tsx` no tenía `role="dialog"`/`aria-modal`/`aria-labelledby`/`aria-describedby` — sin eso, ubicar el modal en el test de forma confiable requería una travesía frágil del DOM (`closest("div").parentElement`, que se rompió en el primer intento porque los botones están en un `<div>` hermano del mensaje, no un ancestro). Se agregó la semántica ARIA real (con `useId()` para los IDs, mismo patrón ya usado en `Combobox.tsx` del punto 24) — mejora de accesibilidad genuina, no solo un parche para que pasara el test; el test ahora usa `getByRole("dialog")`, que es además la forma correcta y estable de ubicar un modal en cualquier test futuro.

**Wireado en CI**: nuevo step `npm test` en `.github/workflows/ci.yml` (job `frontend`, después de `build`, antes de `npm audit`) — sin esto, tener los tests solo serviría si alguien se acuerda de correrlos a mano.

**Verificado con la lección del punto 17/18 aplicada** (no repetir el error de verificar en un entorno que no coincide con el de CI): se borró `node_modules` y se corrió `npm ci` limpio (no `npm install` sobre lo ya instalado, que puede ocultar problemas de lockfile) y se reprodujo la secuencia exacta del job `frontend` del CI en orden — `lint` → `build` → `test` → `audit` — los 4 pasos limpios contra la instalación fresca.

Con esto se cierra la **Fase 3** (puntos 21-25) completa — los 5 puntos quedaron ✅ resueltos, ninguno parcial.

---

## Fase 4 — Expansión de producto (alcance nuevo, no bugs — solo después de cerrar 0-3)

Estos ítems no son correcciones de algo roto, son capacidades nuevas. Tiene sentido dejarlos al final: construirlos sobre una base sin RBAC/auditoría/reconciliación real solo multiplicaría el riesgo ya identificado.

| # | Tarea | Depende de | Esfuerzo | Informe |
|---|---|---|---|---|
| **26** | Decidir si la nómina electrónica DIAN es requisito de negocio vigente; si lo es, conectar `erp/internal/payroll` con `cofacture/payroll` + `cofacture/soap` (el builder y el CUNE ya existen, están huérfanos) antes de destrabar el frontend de RRHH/Nómina. | Fases 1-3 cerradas | Alto | 04, 09 |
| **27** | Planificar eventos RADIAN (acuse de recibo, recibo del bien, aceptación/rechazo) si el negocio necesita que las facturas operen como título valor — hoy no hay ningún punto de partida en el código. | Fase 1 cerrada | Alto | 04 |
| **28** | Costeo de inventario (mínimo promedio ponderado) — requisito previo para cualquier reporte de rentabilidad real. | — | Alto | 09 |
| **29** | CRM comercial como módulo nuevo (`erp/internal/crm`), reutilizando `thirdparty` como base de contacto y el mismo patrón de puertos que ya usan `sales`/`purchase`/`inventory`. | Fase 2 cerrada (para que nazca ya con el RBAC/auditoría correctos, no heredando el problema) | Alto | 09 |
| **30** | Reportes operativos cruzando módulos (top clientes/productos, rotación de inventario, comparativo ventas-compras). El módulo `stats` que se pensaba ampliar para esto se eliminó el 2026-08-09 (quedó huérfano, sin uso real — ver punto 15); si esta necesidad se vuelve concreta, evaluar de nuevo en ese momento si conviene un módulo transversal de solo lectura (patrón CQRS válido para agregaciones multi-schema) o extender los dashboards client-side existentes por módulo. | 28 (para que los reportes de rentabilidad sean correctos desde el día uno) | Alto | 09 |

---

Informes de origen: [01-arquitectura](01-arquitectura.md) · [02-base-de-datos](02-base-de-datos.md) · [03-backend-go](03-backend-go.md) · [04-cofacture-dian](04-cofacture-dian.md) · [05-frontend](05-frontend.md) · [06-seguridad](06-seguridad.md) · [07-testing-calidad](07-testing-calidad.md) · [08-devops-produccion](08-devops-produccion.md) · [09-roadmap-erp](09-roadmap-erp.md) · [00-resumen-ejecutivo](00-resumen-ejecutivo.md)
